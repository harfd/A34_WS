<launch>
    <node name="tf_broadcaster_node" pkg="tf_broadcaster" type="tf_broadcaster" output="screen">
        <rosparam command="load" file="$(find tf_broadcaster)/config/tf_broadcaster.yaml" /> <!--Load parameters from config files-->
    </node>
</launch>
